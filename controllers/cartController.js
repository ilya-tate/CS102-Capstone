const createCart = (req, res) => {
  res.send('Cart');
};

const deleteCart = (req, res) => {
  res.send('Cart');
};

const getCart = (req, res) => {
  res.send('Cart');
};

const updateCart = (req, res) => {
  res.send('Cart');
};

module.exports = { createCart, deleteCart, getCart, updateCart };
